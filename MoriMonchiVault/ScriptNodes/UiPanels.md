---
tags: [script, ui, helper]
---

# UiPanels.cs

**Ruta:** `UI/UiPanels.cs`

**Responsabilidad:** Helper estático con utilidades de paneles UITK. `RootOf()` para resolver `rootVisualElement`, `SetActiveIndex()` para toggled de clases CSS, `ClampSelection()` para aritmética de índices.

**Vinculado a:** [[Index/05 - UI System]]

**Conexiones:** [[CreatureGridUITK]], [[HotbarHUDUITK]], [[StoragePanelUITK]], [[NameTag]], [[NpcThoughtTag]]
