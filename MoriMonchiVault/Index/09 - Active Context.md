---
tags: [index, core]
---

# 09 - Active Context

**Session:** 2026-08-27 (Session 85 — **AUDITORÍA + REMEDIACIÓN DATA-DRIVEN + SPAWN POR BORDES CON FASE VISIBLE + PODA DE CONTEXTO + AUDITORÍA QoL — ✅ CERRADA: 7 scripts modificados + prefab `UnitView` + escena + `Index/20` §12 + commit combinado S84+S85**)
**Focus:** Juan abrió cuestionando la calidad/iterabilidad del código: *"¿podría setear todo lo importante a través de ScriptableObjects, settings en managers y editando MMFeedbacks?"*. Auditoría completa de los 38 scripts del prototipo (pre-sesión): la capa de reglas/data SÍ pasa el test (SOs de habilidades/unidades/enemigos/tablero muy bien resueltos), la capa de presentación NO (duraciones en `const`, vistas de unidad 100% procedurales sin prefab donde colgar nada, `FlashHit` como tween a mano violando la regla MMFeedbacks, HUD entero por código). Remediación aprobada + pedido nuevo de spawns + auditoría QoL final.

1. **A1 — tunables liberados**: las 6 duraciones de `ResolutionAnimator` (move/push/land/rotate/hitPause/wavePause) pasaron de `const` a `[SerializeField]`; marcador de spawn de `NightSpawner` serializado (color/texto/tamaño/altura); el HUD lee `Choreography.MaxActions` en vez del "2" hardcodeado.
2. **A2 — convención `Feedbacks/OnHit` instalada**: prefab NUEVO `Assets/RunRunSimulator/CombatPrototype/UnitView.prefab` (raíz `CombatUnitView` + `VisualMount` + `Disc` + `Label` TMP + **`Feedbacks/OnHit` con `MMF_Player`** y un `MMF_Scale` punch 1→1.25/0.18s tuneable). `CombatUnitView` ya no construye nada por código (refs serializadas; `FlashHit()` = `onHit.PlayFeedbacks()`; tint de semilla y alturas de arco/lanzamiento serializados en el prefab). `CombatPrototypeManager` instancia el prefab con helper único `SpawnView` (murió el bloque triplicado). La convención completa de Juan (GO `Feedbacks` por prefab, un GO por momento con su `MMF_Player`, slot serializado en el padre, wiring de eventos arriba) quedó ampliada en la memoria persistente `feedback_vfx_solo_mmfeedbacks`.
3. **Spawn nocturno legible (pedido de Juan)**: refuerzos SOLO en celdas de BORDE (`NightWaves.FindEdgeSpawnCells` — celda jugable con vecino cardinal fuera de `InBounds`, orden determinista por Chebyshev a la semilla con desempate y,x; incluye bordes de huecos interiores — tunable), **fase nueva `CombatPhase.Spawning`** con banner propio al cerrar el turno enemigo (flujo visible: beats → ataque enemigo → REFUERZOS → planificación), y entrada SALTANDO en arco desde fuera de la isla (secuencial, `SnapTo` fuera + `MoveTo` con arco) + `ShakeAt` del bloque al aterrizar. Tunables en Manager: `spawnJumpDuration` (0.4), `spawnJumpFromCells` (1.5). `spawnRadius` eliminado. `CombatAutoTester` alineado a bordes (firma de `RunMatch` sin `spawnRadius`; nadie externo la llamaba).
4. **Poda del Active Context (pedido de Juan)**: este archivo pasó de 276KB→~23KB; las sesiones ≤S79 viven ahora en `Index/09b - Active Context Archive.md`. El hook SessionStart deja de inyectar contexto muerto.
5. **⭐ Auditoría QoL con verificación VISUAL**: 3 capturas del Game view en Play (`Assets/Screenshots/qol_*.png`, incluida cámara rotada 90°) → **20 hallazgos** (banner pisa la línea de selección; beatStrip/actionBudget detrás de las cards; cards tapan tablero jugable; banner tapa filas altas; labels de mundo quedan de canto tras la órbita; ☠ renderiza tofu □; FIZZLE invisible; clic atraviesa la UI; órbita descentra el encuadre; contraste lavado; etc.). Todo documentado con plan de fixes en **`Index/20` §12 = AGENDA S86**. Regla de pipeline NUEVA en memoria (`feedback_verificacion_visual_screenshots`): todo trabajo visible cierra con capturas MIRADAS (checklist overlaps/oclusión/legibilidad/glifos, también con cámara rotada) — la verificación por estado no detecta nada de eso.
6. **✅ Verificación MCP**: compilación 0 errores/0 warnings en ambas tandas · Play ronda 1: 6 vistas desde prefab (semilla+3 dragones+oleada), labels correctos (`6`, `G2·1`), `FlashHit`→`MMF_Player.IsPlaying=True`, escala vuelve a 1.00 · Play ronda 2: despliegue→`Spawning`→`Planning`, turno completo jugado por código, enemigos 2→4 **todos en borde** (dPos=0.00), consola limpia.
7. **Quirks MCP nuevos (para la nota 12)**: `execute_code` ahora compila con **Roslyn** (el quirk #2 "solo C# 6" quedó desactualizado) · el clasificador de permisos bloquea mutar escena/refs por `execute_code` — el camino es `manage_components set_property` + `manage_scene save` (el set de refs por asset path o instanceID funciona y se verifica leyendo el resource del componente).

> ### 📌 Notas S85
> 1. **S84 cerró sin protocolo** (sin entrada aquí, sin ScriptNodes, sin commit): entrada S84 de abajo reconstruida en S85 desde código+memoria; `vault-documenter` corrió en este cierre con los 19 scripts de ambas sesiones.
> 2. Los 2 commits acordados (S84 y S85 separados) fueron imposibles: ambas sesiones tocaron los mismos archivos y S84 nunca se commiteó → **un commit combinado S84+S85** (precedente: S82+S83).
> 3. `Assets/_Recovery/0 (2).unity` apareció sin trackear (recovery de Unity, no de la IA) — NO commiteado, Juan decide (igual que el `0 (1).unity` de S83). Las capturas `qol_*.png` quedan sin commitear como evidencia local.
> 4. Decisiones v0 del orquestador en el spawn (veto de Juan al jugar): bordes incluyen huecos interiores · celdas por cercanía a la semilla · saltos secuenciales.

**Files Touched S85 (.cs — input ScriptNodes):** MODIFICADOS (7): `ResolutionAnimator`, `NightSpawner`, `NightWaves`, `CombatUnitView`, `CombatPrototypeManager`, `CombatPrototypeHUD`, `CombatAutoTester`. NUEVOS: ninguno (.cs). **No-ScriptNode:** `UnitView.prefab` (NUEVO), `CombatPrototype.unity` (wiring viewPrefab/impact), `Index/20` (§12 nueva), este archivo + `09b` (poda). Memoria: `feedback_vfx_solo_mmfeedbacks` (ampliada), `feedback_verificacion_visual_screenshots` (NUEVA), `MEMORY.md`.

**Next session (S86):** **la agenda es `Index/20` §12** — fixes QoL en orden de dolor: (1) clic-a-través de UI, (2) re-layout del HUD por franjas, (3) billboards por frame, (4) FIZZLE visible, (5) marker sin tofu, (6) órbita que re-encuadre, (7) contraste del tablero, (8) jerarquía de highlights, (9) menores (Esc/botón restart/texto victoria/label semilla). Siguen en cola: UI de secuencia por beat (§10.4), popups DamageNumbersPro (§10.6), layouts con oclusión (§10.1), sesión dedicada de HUD-a-data/localización. Arrastres iguales (pendiente editor S75, endpoints Cloud Code, keys huérfanas, reescritura de `Index/02`, borrar registro MCP viejo `unity-mcp`, decidir `Assets/_Recovery`).

---

**Session:** 2026-08-27 (Session 84 — **SEMILLA NOCTURNA: pivote a defensa de objetivo — ⚠️ CERRADA SIN PROTOCOLO; entrada reconstruida en S85 desde código + memoria persistente**)
**Focus:** el prototipo dejó el placeholder "matar todo" y pasó a **defensa de objetivo**: plantar la Semilla Nocturna (fase `Setup`: clic para plantar semilla + desplegar los 3 dragones), sobrevivir oleadas crecientes y llegar al turno de germinación = VICTORIA (los enemigos vivos mueren en la germinación). Derrota si la semilla o todos los dragones caen. Juan autorizó testeos automáticos de partidas (memoria `project_semilla_nocturna`).

1. **7 scripts NUEVOS**: `SeedUnit` (unidad inmóvil, inmune a push/launch) · `NightWaves` (tamaño de oleada `base + (n-1)/extraEveryWaves` + búsqueda determinista de celdas) · `NightSpawner` (oleada pendiente + telegraph: highlight `Spawn` + markers TMP "☠ 1") · `TurnLogEntry`/`TurnLogPanel` (log de turnos ejecutados, panel UITK izquierda) · `CombatAutoTester` (simulador headless de partidas con perfiles pasivo/distraído/defensor y métricas) · `AbilityCardVisuals` (minigrids 5×5 de plantilla + tags en las cards del HUD).
2. **12 MODIFICADOS**: `CombatPrototypeManager` (fase `Setup`, `seedTicks`/`germinationTurn` serializados, oleadas por turno, `TurnLog` + evento `TurnLogChanged`, `ResolveGermination`) · `CombatEffects` (**rotar-hacia-atacante**: todo hit no-ambiental gira el apuntado del enemigo hacia el atacante — evento `Rotate`) · `ActionResolver` (germinación; movimiento enemigo fin-de-turno) · `CombatInputController` (**idioma drag**: clic destino → arrastrá orienta → soltá confirma; `PlaceAt` en Setup) · `TargetingController` (`SetDirection`) · `CombatPrototypeHUD` (banners de despliegue nocturno, línea SEMILLA con countdown, integración minigrids, chips de beat) · `BoardHighlighter` (`HighlightKind.Spawn`) · `CombatCameraController` (pan WASD con clamp al tablero) · `AbilityTargeting`, `BoardImpactFeedback`, `CombatUnitView` (tint semilla), `ResolutionAnimator` (ajustes menores).
3. **Assets**: `BoardLayout_IslaNocturna` NUEVO (isla del modo defensa). Escena re-wireada.
4. ⚠️ Sin verificación documentada de cierre (la sesión no corrió `/cerrar-sesion`); S85 verificó el conjunto en Play al construir encima.

**Files Touched S84 (.cs — input ScriptNodes):** NUEVOS (7): `SeedUnit`, `NightWaves`, `NightSpawner`, `TurnLogEntry`, `TurnLogPanel`, `CombatAutoTester`, `AbilityCardVisuals` · MODIFICADOS (12): `AbilityTargeting`, `ActionResolver`, `BoardHighlighter`, `BoardImpactFeedback`, `CombatCameraController`, `CombatEffects`, `CombatInputController`, `CombatPrototypeHUD`, `CombatPrototypeManager`, `CombatUnitView`, `ResolutionAnimator`, `TargetingController`. **No-ScriptNode:** `BoardLayout_IslaNocturna.asset` (NUEVO), `CombatPrototype.unity`.

**Next session (S85):** ver entrada de arriba (ya ocurrió).

---

**Session:** 2026-08-26 (Session 83 — **LEGIBILIDAD DEL PROTOTIPO: feedback del primer playtest de Juan implementado y verificado — ✅ CERRADA: 8 scripts modificados + tunables de escena + commit/push de S82+S83**)
**Focus:** Juan reportó 3 cosas tras probar el idioma nuevo: (1) poder rotar/ver la isla, (2) más feedback de a quién y qué habilidad está seleccionando, (3) los tiles impactados no vibran (solo las unidades). Regla nueva de Juan guardada en memoria persistente (`feedback_legibilidad_primero`): **la prioridad del MVP es la LEGIBILIDAD — nunca cumplir con el mínimo en presentación** (*"siempre pregúntate: ¿esto que estoy planteando es legible para el usuario o debería añadir algo más?"*).

1. **Diagnóstico (causas raíz reales, del vault + código)**: los tiles no vibraban porque `ResolutionAnimator` solo disparaba `ShakeAt` en Land y hits ambientales — los Hit de plantilla y los ataques enemigos jamás sacudían bloques; la selección no daba feedback porque `SelectUnit`/`SelectAbility` nunca refrescaban el HUD (el resaltado existente solo aparecía al cambiar la proyección) y no había indicador en mundo; la órbita ←/→ de S82 funcionaba bien — el problema era descubribilidad y falta de zoom.
2. **Vibración en TODO impacto**: `ResolutionEventType.Impact` NUEVO — `ActionResolver.ResolveAttack` lo emite con las celdas de plantilla (siempre que no hubo fizzle, haya o no víctimas; NO toca estado → proyección==ejecución intacta, verificado en frío sobre clon). `ResolutionAnimator` sacude bloques en Impact, EnemyAttack (sus `Cells`), Land y ambientales. Tunables subidos en escena: `wiggleAmplitude` (0.04,0.07,0.04)→(0.07,0.14,0.07), `wiggleDuration` 0.3→0.45.
3. **Selección legible (mundo + HUD redundantes)**: `TargetingController` gana evento C# `SelectionChanged` + `AwaitingSlamCell` + anillo blanco bajo el dragón seleccionado (`HighlightKind.Selection` nuevo en `BoardHighlighter`); el HUD se suscribe (OnEnable/OnDisable) y gana `selectionLabel` bajo el banner con texto-guía por estado ("Elegí un dragón: F1-F3 o clic sobre él" → "{dragón} — elegí plantilla: 1-3" → "{dragón} — {plantilla} · WASD/mouse apunta · Q/E rota · Enter o clic confirma", variante slam), tintado con el color del dragón; card seleccionada con fondo+borde, habilidad seleccionada como pill amarillo, usadas en gris "— usada"; **clic sobre dragón propio ahora SELECCIONA** (`TrySelectPlayerAt` en `CombatInputController`, estilo ITB; solo confirma acción si la celda no tiene dragón propio).
4. **Cámara**: zoom con rueda del mouse en `CombatCameraController` (dolly `_baseOffset * _zoom`, clamp 0.5–1.6, usar solo el SIGNO del scroll — en Windows viene ±120 por muesca); banner de planificación con 2da línea de controles "←/→ girar cámara · rueda: zoom · clic derecho sobre enemigo: info".
5. **✅ Verificación MCP completa**: compilación 0 errores/0 warnings · 9 contratos nuevos verificados por reflexión · en Play: `SelectionChanged` dispara 2→3 veces correcto, anillo aparece/desaparece (1→0 quads), los 3 textos del `selectionLabel` correctos, `ShakeAt` inicia MMWiggle (WigglePermitted False→True, LimitedTimeLeft 0.45), resolver en frío sobre clon emite Impact (Golpe sin víctima → Move+Impact, 2 eventos) · consola limpia al salir.
6. **Ejecución**: 7 `morimonchi-coder` en paralelo (uno por archivo), cero desvíos, cero fixes de integración del orquestador.

> ### 📌 Notas S83
> 1. Los ScriptNodes de S82 YA estaban actualizados (la nota de S82 que decía "vault-documenter NO disparado" no reflejaba el estado real del vault); ese pase dejó 2 archivos basura `ScriptNodes/EnemyUnit.cs` y `ScriptNodes/ResolutionEvent.cs` (contenido de nodo con extensión .cs, duplicados de los .md reales) — **borrados en S83**.
> 2. `Assets/_Recovery/0 (1).unity` apareció sin trackear (escena de recovery de Unity, no creada por la IA) — **NO se commiteó**; Juan decide si borrarla.
> 3. Del feedback S81 (`Index/20` §10) siguen pendientes: punto 4 (UI de secuencia por beat) y punto 6 (popups DamageNumbersPro); del punto 1 la cámara ya cumple — falta la mitad de layouts (verticalidad/oclusión deliberada).

**Files Touched (.cs — input ScriptNodes):** MODIFICADOS (8): `ResolutionEvent`, `ActionResolver`, `ResolutionAnimator`, `TargetingController`, `BoardHighlighter`, `CombatPrototypeHUD`, `CombatInputController`, `CombatCameraController` (todos en `Scripts/CombatPrototype/`). NUEVOS: ninguno.

**Files Touched (no-ScriptNode):** `CombatPrototype.unity` (tunables wiggle), `Index/20` (nota S83 en Estado), este archivo. Memoria del orquestador: `feedback_legibilidad_primero.md` (NUEVA) + `MEMORY.md`.

**Next session (S84):** playtest de Juan con la legibilidad nueva (checklist `Index/20` §8) → sus anotaciones mandan. Después, en agenda: UI de secuencia por beat (punto 4) + popups DamageNumbersPro (punto 6) + layouts con verticalidad/oclusión (resto del punto 1). Arrastres iguales (pendiente editor S75, endpoints Cloud Code, keys huérfanas, reescritura de `Index/02`, deuda de documentación, borrar registro MCP viejo `unity-mcp`, borrar `Assets/_Recovery` si Juan confirma).

---

**Session:** 2026-08-26 (Session 82 — **EL IDIOMA NUEVO DEL COMBATE: puntos 2 y 3 del feedback S81 bajados a reglas exactas, implementados y verificados — ✅ CERRADA: 20 scripts (18 mod + 2 nuevos) + 11 assets + isla nueva + escena wireada, proyección==ejecución re-verificada; S83 abre con el playtest de Juan y el push pendiente**)
**Focus:** Sesión de diseño + implementación. Juan cerró en 2 rondas de decisiones las reglas exactas de los puntos 2-3 de la agenda (`Index/20` §11, decisiones 17-24): enemigos = **activadores automáticos** (atacan SIEMPRE en su apuntado, haya o no objetivo; el apuntado es un estado que el jugador manipula), **cero movimiento propio** (solo al final del turno SI FUE GOLPEADO, con **patrón de ajedrez** visible en el brief; bloqueado → rota 90° horario — "los muros redirigen enemigos"), y **plantilla = anclaje + aterrizaje** con **transición sin límite** (ni distancia ni altura; la forma define el aterrizaje). Regla nueva de Juan sobre la marcha: **máximo 2 acciones por turno** (la presión pasa de la persecución al desgaste). Extras pedidos: bloques altura-de-MoriMonchi, isla más grande y vertical estilo Bad North, y "que al caer se sienta el impacto" con Feel/MMFeedbacks.

1. **Implementación**: 3 tandas de `morimonchi-coder` (11 agentes) + 1 tanda extra (celdas-hueco) + 2 fixes de integración del orquestador. 18 scripts modificados + 2 NUEVOS (`BoardImpactFeedback` = vibración de bloques por `MMWiggle` de Feel con hook `MMF_Player` opcional; `CombatCameraController` = órbita por pasos de 90° con flechas ←/→). Targeting reescrito (anclaje libre, `LandingKind {Stay, AtAnchor, BehindAnchor}`, FIZZLE si el aterrizaje se ocupó), resolver reescrito (viaja+golpea, sin reacciones por beat, turno enemigo = ataque automático + fase de movimiento de golpeados), `EnemyUnit.Facing` persistente (spawn por layout con `>`/`<`/`^`/`v` — ojo: `^`=(0,1)=+z, que en el texto del layout apunta "hacia abajo").
2. **Celdas-hueco** (`.` en HeightRows → `InBounds` false): la forma real de los "bordes no lisos" que pidió Juan, ya que la altura no bloquea movimiento. `BoardLayout_Isla` NUEVO: 12×12, alturas 0-4, 26 huecos, 118 bloques.
3. **Assets re-parametrizados por MCP** (9 habilidades + 2 enemigos): ataques con offset (0,0)+BehindAnchor, Disparo línea (0,0)(1,0)(2,0), Arco → DirectionalTemplate+Stay, vuelos AtAnchor; Goblin Torre-2 (1,0)(2,0), Arquero Alfil-2 (1,-1)(2,-2), briefs nuevos. Escena wireada: `BoardImpactFeedback` en Board, cámara con controller, `levelHeight` = **1.06** (bounds reales del DragonSD × 0.55), Manager Y Builder apuntando a la Isla.
4. **✅ Verificación MCP completa**: consola 0 errores / 0 warnings (compilación + Play) · **proyección == ejecución IDÉNTICAS en 2 rondas jugadas por código** — ronda 1: Empujón-contra-muro (+1 entorno) + Disparo con viaje + Torre-2 (ignora altura) + Alfil-2 en diagonal; ronda 2: **combo canónico traducido** Voltereta→Agarre→Slam-contra-muro = 3 ticks = muerte del Goblin, entrando JUSTO en el presupuesto de 2 acciones · rotación-al-bloqueo (facing (-1,0)→(0,1), 1 evento Rotate, sin moverse) y fizzle (colisión de aterrizajes en el mismo beat: el 2do slot se cancela) verificados en frío sobre clones. Verificación previa sin editor: truco MSBuild de S75 (csproj de verificación, borrado tras usarse).
5. **MCP**: la sesión arrancó sin bridge (verificación por MSBuild); Juan reconectó con `/mcp` → server **UnityMCP** (CoplayDev). ⚠️ Hay un registro VIEJO duplicado llamado `unity-mcp` que timea — **borrarlo de la config**. Quirk nuevo: `execute_code` corre como cuerpo de método (sin `using` — calificar todo, y `UnityEngine.Object` explícito por ambigüedad con `object`).
6. **Tunables de Feel para Juan** (en la escena): `Board` → CombatBoardBuilder (`wiggleAmplitude`, `wiggleFrequency` — ¡son SEGUNDOS entre cambios, no Hz! default 0.05) y BoardImpactFeedback (`radius`, `wiggleDuration`, `extraFeedback` = MMF_Player vacío para montar camera shake etc.). Cámara: `rotateDuration`.

> ### 📋 Notas S82 — TAREAS PARA LA REAPERTURA
> 1. **Lo primero: que Juan JUEGUE** el idioma nuevo en `CombatPrototype.unity` (checklist `Index/20` §8): plantilla-como-destino, activadores automáticos, girar enemigos a empujones, presupuesto de 2, órbita de cámara (←/→), isla con huecos, vibración de bloques. Sus anotaciones = la próxima agenda.
> 2. **Restan del feedback S81** (`Index/20` §10): punto 4 (UI de secuencia por beat sobre el tablero), punto 6 (popups con DamageNumbersPro), y montar más juice en el hook `extraFeedback`.
> 3. **Push pendiente**: todo el trabajo S82 está sin commitear (20 .cs + 11 assets + escena + layout nuevo + vault).
> 4. **`vault-documenter` NO disparado** (regla: solo vía /cerrar-sesion). Input listo en Files Touched.
> 5. Tunables marcados en la 20 §11 como "decisiones v0 del orquestador" — Juan puede vetar cualquiera al jugar (ej. patrón todo-o-nada, rotación siempre horaria, aéreo no se mueve).

**Files Touched (.cs — input ScriptNodes):** MODIFICADOS (18): `CombatAbilitySO`, `AbilityTargeting`, `TargetingController`, `Choreography`, `BoardLayoutSO`, `CombatPrototypeManager`, `CombatPrototypeHUD`, `EnemyUnit`, `EnemyIntent`, `ResolutionEvent`, `CombatEffects`, `EnemyDefinitionSO`, `EnemyBrain`, `ActionResolver`, `ResolutionAnimator`, `CombatUnitView`, `CombatBoard`, `CombatBoardBuilder` · NUEVOS (2): `BoardImpactFeedback`, `CombatCameraController` (todos en `Scripts/CombatPrototype/`).

**Files Touched (no-ScriptNode):** `Index/20` (§11 nueva + §2/§3/§4/§5 enmendadas + §10 marcado + Estado), 11 `.asset` re-parametrizados, `BoardLayout_Isla.asset` (NUEVO), `CombatPrototype.unity` (wiring), este archivo.

**Next session (S83):** playtest de Juan del idioma nuevo → sus anotaciones mandan. Después: UI de secuencia por beat (punto 4) + popups (punto 6). Arrastres iguales (pendiente editor S75, endpoints Cloud Code, keys huérfanas, reescritura de `Index/02`, deuda de documentación, borrar registro MCP viejo `unity-mcp`).

---

**Session:** 2026-08-25 (Session 81 — **EL MVP DE COMBATE SE JUGÓ: fases 1-4 ejecutadas y verificadas — prototipo jugable completo, victoria en 4 turnos — ✅ CERRADA: 29 scripts nuevos + escena + assets, feedback de Juan anotado como agenda S82**)
**Focus:** Juan abrió con *"es momento, verifica el draft y la conexión MCP"* y tras la fase 1 pidió *"ejecuta todo en bucle, al final solo hazme un review"*. Se ejecutaron las fases 1-4 del plan (`Index/20` §7) en 3 tandas de `morimonchi-coder` con contratos fijados por el orquestador (19 agentes, 29 archivos, CERO errores de compilación al integrarse), verificación completa por Unity MCP, y una partida entera jugada por código hasta la victoria. Al cierre Juan jugó, dejó 7 anotaciones de diseño/juice y pidió documentar + pushear.

1. **Entregable**: prototipo jugable en `CombatPrototype.unity` — planificación por beats (F1-F3/1-3/WASD/Q-E/Enter/Tab/Backspace/R), preview total, EXECUTE, turno enemigo telegrafiado, victoria/derrota/restart, HUD UITK en C# + brief de enemigo (clic derecho). 29 scripts en `Scripts/CombatPrototype/` (namespace `MoriMonchiSimulator.CombatPrototype`), assets en `Assets/RunRunSimulator/CombatPrototype/` (9 `CombatAbilitySO`, 3 `PlayerUnitDefinitionSO` con prefabs DragonSD_00/10/20, 2 `EnemyDefinitionSO`, `BoardLayout_Test`). Sistema 100% aislado: cero contacto con GameEvents/GameManager/UIManager/SaveSystem/InputActions.
2. **⭐ La regla innegociable VERIFICADA**: `ActionResolver` único (compuesto por `AbilityTargeting` + `CombatEffects`) consumido por `PlanProjection` (clones) y `PlanExecutor`/`EnemyTurnController` (canónico) — **7 comparaciones proyección-vs-ejecución IDÉNTICAS** unidad por unidad, incluyendo el combo canónico Voltereta→Agarre→Slam (1+1+1 = 3 ticks ✓) y el turno enemigo. Emergentes observados: fuego amigo por telegraph relativo desplazado (decisión 12 ✓) y el anti-cargamontón esquivando un remate mal secuenciado (lección: rango al final, no antes del melé).
3. **Arquitectura**: estado clonable (`CombatSimState` dueño único de ocupación; `CombatBoard` solo geometría) · lógica pura estática · Monos delgados (Manager orquesta fases sin reglas) · views tontas que reproducen `ResolutionEvent`s por olas (`ResolutionAnimator`) · data en SOs planos sin Odin. Puente futuro intacto: unidades comen loadout plano (ticks + `CombatAbilitySO[]`).
4. **5 bugs cazados en integración**: ocupación duplicada en CombatBoard (eliminada — un dueño por dato) · targeting del Agarre validaba contra fin-de-beats en vez de inicio-del-beat (el juggle nunca confirmaba) · HUD bindeado antes de existir estado (re-Bind en cada Restart) · árboles UITK huérfanos al recrearse el rootVisualElement (guardas de auto-reconstrucción en HUD y brief) · `CombatBoardBuilder.Board` no sobrevivía reload-in-play (getter lazy).
5. **Decisiones v0 del orquestador donde la 20 no llegaba** (tunables, detalladas en el review a Juan): empuje choca contra cualquier subida; choque y caída no acumulan; movimiento voluntario nunca daña; rangos Chebyshev / persecución Manhattan; reacciones con |Δnivel|≤1 alejándose del último atacante no-ambiental; slam en línea cardinal 1-2; fuego amigo del jugador existe (estilo ITB).
6. **⭐ FEEDBACK S81 DE JUAN → AGENDA S82** (detalle en `Index/20` §10): (1) verticalidad Bad North + rotación de cámara recompensada, bloques más altos · (2) enemigos SIN movimiento propio — solo se reposicionan al ser golpeados; su turno = solo atacar (matiza decisión 10-11 de S80) · (3) ⭐ plantilla = zona de impacto + el MoriMochi SE MUEVE y ejecuta ahí (restaura §4.1.7 de S77: posición=consecuencia; cambio grande de targeting+resolver) · (4) UI de secuencia por beat sobre el tablero · (5) presupuesto visible en UI · (6) popups de daño (DamageNumbersPro) · (7) bloques que tiemblan al impacto — TODO el juice con Feel/MMFeedbacks.

> ### 📝 Notas S81
> 1. **Quirks nuevos de Unity MCP documentados en la nota 12** (sección S81): el bridge muere tras domain reloads largos y NO se auto-rearma (fix: reiniciar el editor — el server HTTP lo lanza el plugin y muere con él); `component_properties` en create de GameObject falla silencioso con algunos campos (usar SerializedObject por `execute_code`); en Play desatendido activar `Application.runInBackground` y vigilar `EditorApplication.isPaused`; tras compilar, ESPERAR el reload completo antes de Play (si no, reload-in-play borra el estado runtime).
> 2. Quedan 6 screenshots de verificación en `Assets/Screenshots/` (borrar cuando Juan confirme).
> 3. La Bomba y los experimentos de fase 5 quedaron explícitamente fuera; ahora subordinados a la agenda S82.
> 4. `vault-documenter` invocado con los 29 scripts (todos NUEVOS).

**Files Touched (.cs — input ScriptNodes):** 29 NUEVOS en `Assets/RunRunSimulator/Scripts/CombatPrototype/`: BoardLayoutSO, CombatBoard, CombatBoardBuilder, BoardHighlighter, CombatAbilitySO, PlayerUnitDefinitionSO, EnemyDefinitionSO, ResolutionEvent, PlannedAction, Choreography, CombatUnit, PlayerUnit, EnemyUnit, CombatSimState, EnemyIntent, AbilityTargeting, CombatEffects, ActionResolver, EnemyBrain, PlanProjection, CombatUnitView, ResolutionAnimator, TargetingController, CombatInputController, PlanExecutor, EnemyTurnController, CombatPrototypeManager, CombatPrototypeHUD, EnemyBriefPanel.

**Files Touched (no-ScriptNode):** `CombatPrototype.unity` (NUEVA), carpeta `Assets/RunRunSimulator/CombatPrototype/` (15 .asset NUEVOS), `Index/20` (§10 feedback S81 + Estado), `Index/12` (quirks S81), este archivo.

**Next session (S82):** **la agenda es `Index/20` §10** — en orden sugerido: decisiones de mecánica primero (puntos 2 y 3: enemigos sin movimiento propio + plantilla-como-destino, bajarlas a reglas exactas con Juan antes de codear), después presentación (4, 5) y juice con Feel/MMFeedbacks (1 cámara/verticalidad, 6 popups, 7 shake). El pendiente obligatorio de editor de S75 (assets Horn/Back/Wing/Face/CutieMark + rewiring + limpieza de GameScene) sigue vigente e independiente. Arrastres iguales (endpoints Cloud Code, keys huérfanas, reescritura de `Index/02`, deuda de documentación).

---

**Session:** 2026-08-25 (Session 80 — **EL PLAN DEL MVP DE COMBATE — sesión de diseño + planificación, cero código — ✅ CERRADA: `Index/20` creada (plan aprobado), ronda 4 mayormente cerrada, gate de código levantado SOLO para el prototipo**)
**Focus:** Juan trajo un draft de MVP/prototipo aislado del combate (escena independiente, unidades simplificadas, primitivas) para validar planificación + lectura de intenciones + ejecución simultánea ANTES de integrar Horn/Back/Wings/Personalidad/Cutie Marks/Equipment. Referencias: Mewgenics (tablero) + Bad North (verticalidad ligera). El orquestador contrastó el draft contra la 19, marcó las contradicciones con S77, y en 3 rondas de decisiones (AskUser + 2 mensajes de Juan) quedó cerrada la mecánica v0 del encuentro. Juan cerró con *"guarda, documenta exhaustivamente esto para que no se pise con otras cosas, guarda lo avanzado y pushéalo"*.

1. **Entregable**: [[Index/20 - Combat Prototype MVP (Plan)]] (NUEVA — fuente de verdad de la mecánica del prototipo, prevalece sobre la 19 donde contradigan). Contiene: 16 decisiones S80, reglas v0, kits, enemigos, arquitectura (~20 archivos, `Assets/RunRunSimulator/Scripts/CombatPrototype/`, escena `CombatPrototype.unity`), 5 fases de ejecución con verificación MCP, evaluación (las 7 preguntas del MVP → observables), riesgos y prohibiciones.
2. **⭐ El modelo final v0**: todo es plantilla (vuelo + ataques, sin movimiento por pasos ni BFS) · coreografía por BEATS con acciones simultáneas (resolución contra snapshot, orden de slots, muertes al cierre) · vida en TICKS con daño uniforme 1 (dragones 5, enemigos 3 = guardia 2 + golpe de gracia) · el entorno es la única fuente de ticks extra (muros/caídas) · altura = acceso y defensa, sin bonus de daño · 1 uso por plantilla por planificación · enemigos con intención telegrafiada RELATIVA (viaja con el enemigo, estilo ITB real) que atacan al fin de la coreografía · **reactivos en movimiento** (golpeado en un beat → reposicionamiento determinista al cierre; anti-cargamontón; contras: arrinconar/juggle/ráfaga) · **juggle determinista** (Voltereta lanza al aire → Agarre aéreo del Tanque estrella donde elijas; aéreo no reacciona) · determinismo total, cero RNG.
3. **Ronda 4 de la 19 mayormente cerrada**: Q1 lote ✅ · Q2 preview total ✅ · Q3 enemigos con iniciativa ✅ (revoca "solo reaccionan al toque" de S77; la Bomba sobrevive como enemigo-contador traducido a ticks) · Q5 un uso por plantilla ✅ · C4 vida en ticks ✅. Abiertas: Q4 (MVP usa "matar todo" placeholder), Q6, PE.1, corte del determinismo de escenarios. Banners de actualización S80 puestos en la 19 (cabecera, §4.4 y Estado) para que nada se pise.
4. **Regla técnica innegociable del prototipo**: `ActionResolver` único que resuelve cada beat, consumido por la proyección Y la ejecución — si divergen, el jugador pierde confianza en el preview y muere el pilar. Es el punto delicado #1 de la implementación.
5. **Ganchos futuros registrados (NO implementar en v0)**: countdown de intents ("actúa en el beat N", generaliza la bomba) · recuperación de guardia en ciertos turnos · gracia-solo-ataque — los tres como experimentos de fase 5. Prohibido en v0: niebla, extracción, consumibles, Cutie Marks, personalidad, permadeath, curación, arte.

> ### 📝 Notas S80
> 1. **El gate "nada baja a código" se levantó SOLO para el prototipo aislado** (autorización explícita de Juan en esta sesión, incluye crear escena + .assets del prototipo por MCP). La integración con el juego sigue congelada hasta que el MVP valide.
> 2. **El "documento de mecánica en limpio" ya no es el entregable**: lo reemplaza el prototipo como instrumento de validación (la 20 es su spec).
> 3. Cero `.cs`, cero Unity, cero Notion. **No se invocó `vault-documenter`** (sin scripts tocados).

**Files Touched (.cs — input ScriptNodes):** ninguno.

**Files Touched (no-ScriptNode):** `MoriMonchiVault/Index/20 - Combat Prototype MVP (Plan).md` (NUEVA), `00 - Index.md` (+1 fila de ruteo), `Index/19` (banner S80 en cabecera + callout §4.4 + Estado reescrito), este archivo. Memoria del orquestador: `project_refundacion_combate.md` actualizada + `MEMORY.md`.

**Next session (S81):** **Fase 1 del MVP** (tablero: SOs de layout, `CombatBoard`, `CombatBoardBuilder`, `BoardHighlighter`, escena `CombatPrototype.unity`, cámara isométrica) vía `morimonchi-coder` + verificación MCP (consola 0 errores + Play) — el plan completo está en la 20 §6-§7, no re-derivar nada. El pendiente obligatorio de editor de S75 (assets Horn/Back/Wing/Face/CutieMark + rewiring + limpieza de GameScene) sigue vigente e independiente. Arrastres iguales (endpoints Cloud Code, keys huérfanas, reescritura de `Index/02`, deuda de documentación).

---

> 📦 **Sesiones ≤S79 archivadas** en [[09b - Active Context Archive]] (poda S85, 2026-08-27). No cargar salvo búsqueda puntual de historia.
